name: promptix_ai_flutter
description: "PromptixAI means a smart AI that understands and improves prompts to give better responses."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  rename_app: ^1.6.5
  http: ^1.6.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  google_fonts: ^6.3.3
  shared_preferences: ^2.5.4
  flutter_secure_storage: ^10.0.0
  shadcn_ui: ^0.43.0


  # Core Firebase
  firebase_core: ^4.3.0

  # Firebase Services
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  flutter_native_splash: ^2.4.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true

  assets:
    - assets/
